#include <iostream> 
#include <string>
#include "Task 2.h"
#include <math.h>
using namespace std;


/*int main()
{
	Point2D p1;
	p1.x = 5;
	p1.y = 3;
	Point2D p2;
	p2.x = 10;
	p2.y = 1;
	p1.distance(10,0);
	Point2D newPoint = p2;
	cout << "´the new point are " <<newPoint.toString() << endl;
	Point2D newPoint2;
	newPoint2.x = p1.x + p2.x;
	newPoint2.y = p1.y + p2.y;
	cout << "the new object which is created from p1 and p2 by adding is " << newPoint2.toString()<< endl;
	p1.distance(newPoint2.x,newPoint2.y);
	cout << "To compare:\n the new point is " << newPoint2.toString() << "And the other points \n" << p1.toString() << "\n" << p2.toString() << "\n" << newPoint.toString() << endl;

	
}*/